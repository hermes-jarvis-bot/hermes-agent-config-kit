# Reviewed-script lane (see SECURITY.md "Reviewed-script lane" and AGENTS.md's
# quarantine-policy section for the full policy this manifest implements).
#
# This is the ONLY mechanism by which a skill-bundled script may live under
# hermes/skills/**/scripts/ without failing scripts/validate_output.py's
# quarantine-leak check. Every entry here required, before being added:
#   1. a full manual read of the entire script (not a grep-only pass);
#   2. a dependency check (stdlib preferred; any external dependency is
#      documented as an explicit prerequisite, same as this repo already does
#      for install/apply commands);
#   3. confirmation of no destructive filesystem operations, no credential/
#      secret reads or logging, and no undisclosed network calls;
#   4. a path-portability check (no hardcoded Claude-Code-specific paths such
#      as .claude/ or ~/.claude/, unless kept deliberately as documented
#      upstream-source context);
#   5. `python3 -m py_compile` (or the JS/TS equivalent) passing cleanly;
#   6. for Python/JS, this repo's own automated dangerous-pattern and secret
#      scans (validate_reviewed_scripts() in scripts/validate_output.py)
#      passing cleanly;
#   7. never invoked automatically by this adapter or its CI — only ever
#      invoked by an operator or agent at runtime, under Hermes's own normal
#      permission/consent system, exactly like any other bundled skill script
#      (e.g. Hermes's own official xlsx/pdf/linear skills).
#
# A script here is NEVER auto-converted or auto-regenerated by
# scripts/sync_upstream.py. If the upstream source changes (source_sha256 no
# longer matches upstream), the change must be re-reviewed by hand and this
# entry updated deliberately — the same manual-reapproval discipline this
# adapter already applies to hand-adapted markdown sources.

- path: hermes/skills/ai-ml/notebooklm-grounded-research/scripts/verify_notebooklm_setup.py
  source_repo: AnastasiyaW/claude-code-config
  source_path: skills/ai-ml/notebooklm-grounded-research/scripts/verify_notebooklm_setup.py
  source_sha256: 5aed6be10399b6b47981ca54fe569e13f43b1c0fd54afa7c9ff2d787cef0514d
  reviewed_at: "2026-08-04"
  reviewed_by: "operator-approved pilot, hermes-agent-config-kit session"
  summary: >
    Read-only NotebookLM MCP setup verifier. stdlib-only (argparse, json, os,
    shutil, sys, pathlib, tomllib). No network calls, no subprocess/os.system,
    no eval/exec, no writes. Reads a local MCP client config.toml file and
    checks filesystem path existence only; explicitly reports
    secrets_read: false and never reads cookie/token/browser-database
    content. Codex-oriented (reads $CODEX_HOME or ~/.codex/config.toml), not
    Claude-Code-coupled.
  functional_test: >
    Operator-confirmed live execution on 2026-08-04: `python3 <path> --json`
    returned status "not_configured" (rc=2) on a host with no notebooklm MCP
    server registered, as expected; no filesystem writes or network calls
    were observed.

- path: hermes/skills/distill-feedback/scripts/extract_feedback_queue.py
  source_repo: AnastasiyaW/claude-code-config
  source_path: skills/development/distill-feedback/scripts/extract_feedback_queue.py
  source_sha256: a1fcf14310e7a224e969d15fb9d4bbfed025c7aa12b20749d18f377500e90732
  reviewed_at: "2026-08-04"
  reviewed_by: "operator-approved reviewed-script-lane pilot #2, hermes-agent-config-kit session"
  summary: >
    Deterministic, stdlib-only feedback-queue reader (argparse, json, sys,
    datetime, pathlib; tempfile only inside its own self-test). No network
    calls, no subprocess/os.system/eval/exec. Reads
    ~/.claude/feedback/queue.jsonl and the session transcript paths it
    references; only ever appends to ~/.claude/feedback/processed.jsonl via
    mkdir(parents=True, exist_ok=True) + open("a") — never rewrites or
    deletes either file. Depends on an EXTERNAL, non-Hermes prerequisite: the
    queue file is normally populated by a separate Claude-Code Stop hook that
    is not part of this adapter and not installed by Hermes; if nothing
    populates it, this script correctly returns pending: 0 rather than
    erroring or fabricating data. This external dependency is disclosed in
    the ported SKILL.md's "Prerequisite" section, not hidden.
  functional_test: >
    Ran the script's own built-in --self-test on 2026-08-04 (isolated to a
    tempdir it creates itself, no interaction with the real
    ~/.claude/feedback/ directory): passed ("self-test OK"), confirming
    human-turn extraction excludes tool_result/summary/system-reminder
    entries and that mark_processed correctly excludes processed sessions
    from subsequent pending_sessions() calls.
