interface:
  display_name: "Massed Compute Ops"
  short_description: "Manage GPU VMs safely through Massed Compute"
  default_prompt: "Use $massed-compute-ops to inspect capacity, choose a GPU, launch safely, or audit Massed Compute spend."
dependencies:
  tools:
    - type: "mcp"
      value: "massed-compute"
      description: "Massed Compute account and GPU VM operations"
      transport: "streamable_http"
      url: "https://vm.massedcompute.com/api/mcp"
policy:
  allow_implicit_invocation: true
