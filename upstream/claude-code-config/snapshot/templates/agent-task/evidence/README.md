# Evidence

Store raw artifacts here:

- test runner output
- build logs
- screenshots
- browser traces
- API responses
- generated reports
- verifier outputs

Use stable filenames with timestamps or phase names. Reference every important artifact from `trace.jsonl`, `state.json`, or `verdict.json`.
